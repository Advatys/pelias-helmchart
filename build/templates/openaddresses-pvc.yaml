kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: openaddresses 
  annotations:
spec:
  accessModes:
    - ReadWriteOnce 
  resources:
    requests:
      storage: {{ .Values.pv.openaddresses }} 
